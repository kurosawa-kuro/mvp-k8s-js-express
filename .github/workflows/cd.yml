name: CD

on:
  push:
    branches:
      - production
  pull_request:
    branches:
      - production

jobs:
  deployment-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deployment validation
        run: |
          echo "✅ デプロイの基本チェックを実行中..."
          echo "✅ デプロイチェック完了"